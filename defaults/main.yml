---
samba_ferm_dependent_rules:
- type: accept
  protocols: [ tcp, udp ]
  dport: [ 135, 136, 137, 138, 139, 145, 445 ]
  saddr: "{{ samba_ferm_allow|default(['10.0.2.2']) }}"
  accept_any: True
  filename: samba_dependency_accept
  weight: 50
  comment: "Allow connections to the Drupsible Samba shares"
  multiport: True
  role: samba
  name: 'netbios-ssn-ns-dgm_microsoft-ds'
